<a href="{{ path('car_write_review', {'id': car.id}) }}">Write review</a>


{% filter outlet %}

<ul class="list-unstyled">
    {% for review in reviews %}
        <li>
            <div class="card">
                <div class="card-header">
                    Quote {{ random() }}


                </div>
                <div class="card-body">
                    {{ review.review }}

                    <a href="{{ path('car_review_one', {'id': car.id, 'review_id': review.id}) }}">View review</a>
                </div>
            </div>
        </li>
    {% endfor %}

    qqq
    {% endfilter %}
</ul>